import React from "react";
import { StyleSheet } from "react-native";
import { Text } from "react-native-paper";

const AddMealCustom = () => {
  return <Text>Add meal custom</Text>;
};

const styles = StyleSheet.create({});

export default AddMealCustom;
